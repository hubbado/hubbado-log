require "spec_helper"

RSpec.describe Hubbado::Logger do
  it "has a version number" do
    expect(Hubbado::Logger::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
